---
description: Initialize the directory structure for a new feature specification.
---

1. **Plan**: Use `mcp_sequential-thinking_sequentialthinking` to plan the directory structure and scope.
2. Read the instructions in `agent-os/commands/shape-spec/shape-spec.md`.
3. Create the necessary folder structure under `agent-os/specs/[feature-name]/`.
4. Initialize the `requirements.md` and basic metadata files.
